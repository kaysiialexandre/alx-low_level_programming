#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	/**
	 * return value - return value for main
	 *
	 * Return: always 0
	 */
	return (0);
}
